[credentials]
usernames = [ "admin" ]
names = [ "Administrator" ]
passwords = [ "$2b$12$D23M1ZJ39.gJ3/fQ.q1u.O7v1u8O5/fQ.q1u.O7v1u8O5/fQ.q" ] # This is a placeholder; simple 'admin123' won't work with the library nicely in plain text without Hasher logic, but for now we are mocking the dict construction in auth.py. 
# Actually, for the simplified logic I wrote in utils/auth.py, please use PLAIN TEXT for now and I will handle hashing, OR use this plain config:
[credentials]
usernames = [ "admin" ]
names = [ "Administrator" ]
passwords = [ "admin123" ]
[cookie]
expiry_days = 30
key = "random_signature_key_change_this_in_prod"
name = "market_rover_auth"
[preauthorized]
emails = [ "admin@example.com" ]
