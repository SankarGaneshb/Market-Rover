steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '--build-arg', 'REACT_APP_GOOGLE_CLIENT_ID=${_GOOGLE_CLIENT_ID}',
    '-t', '${_IMAGE_TAG}',
    '.'
  ]
images:
- '${_IMAGE_TAG}'
